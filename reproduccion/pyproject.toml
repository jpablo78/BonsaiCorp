[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "bonsai-reproduccion"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = ["ortools>=9.10,<10"]

[tool.setuptools.packages.find]
where = ["."]
include = ["bonsai*"]
